# Project agent memory

This file is the project's committed home for project-intrinsic agent knowledge: build, test, release, architecture, and sharp-edge notes that should travel with the code.

- `proto/` is the only hand-edited code; everything under `packages/typescript/src` and `packages/python/tesla_protocol/*/` is generated. Regenerate with `pnpm install && pnpm generate`; CI fails on any diff, so never edit generated files directly.
- The upstream-sync mechanism: `upstream.json` pins the exact commit (and per-file sha256) `proto/command/` and `proto/telemetry/` are reconciled against; `scripts/upstream_coverage.py` enforces symbol-level coverage against that pin (run it in `--mode pinned` before pushing proto changes) and a weekly `--mode head` run drives reconciliation. Inside those files, `TESLEMETRY-EXT` markers (trailing comment or `BEGIN`/`END` fence) flag additions not present upstream; the coverage gate requires every such addition to carry one. The fence parser is a flat, non-nesting state machine: a `BEGIN` inside an already-open `BEGIN`/`END` span silently closes the outer span early, leaving everything between the inner `END` and the outer `END` unmarked. Adding a new message inside an existing fenced block needs no new fence at all - it inherits coverage from the block it's placed in. To pull in a not-yet-merged upstream change (an open upstream PR against a branch owned by the upstream org, not a fork), pin `upstream.json` straight to that PR's head commit and its file's sha256 (not a TESLEMETRY-EXT extension - these are upstream fields) so `--mode pinned` passes; when upstream later merges the PR with the same file bytes, `update_upstream_pin.py` sees no byte change and reconciliation is a no-op.
- `upstream_coverage.py --dump-delta` writes a JSON reconcile delta classifying each tracked upstream into `coverage_gap`, `wire_conflict`, `upstream_overlap` (a marked local extension upstream now also defines natively) and/or `non_proto_commit_only`. Its `reconcile_needed` field - not the process exit code, which still reflects schema coverage only - is what `upstream-drift.yml` uses to decide whether to advance the pin and open a reconcile PR, because upstream re-publishing an already-local symbol changes the pinned file bytes without changing coverage (a coverage-gap-free state can still need reconciling). `scripts/update_upstream_pin.py` only advances an upstream's commit/fetched/files when its tracked file bytes actually changed, so HEAD movement outside the tracked path prefix never produces a pin diff or a PR. Upstream wins on name/number collisions, except a field whose upstream type is an empty placeholder message (Tesla's `Void {}` convention) never overrides an already-concrete local field at that number.
- Release model: Changesets tracks only the npm package; `scripts/sync-python-version.mjs` copies the version into `pyproject.toml` during `pnpm run version`. Bump rules are in `.changeset/README.md`. Never use changesets prerelease mode (breaks PEP 440). Version bumps follow pragmatic versioning (BIGRELEASE.ANNOUNCE.INCREMENT, https://raw.githubusercontent.com/seveibar/pragmaticversioning/refs/heads/main/README.md), not strict semver: INCREMENT is the default for any ordinary contribution - don't deliberate over whether an addition "counts" as a feature - ANNOUNCE is reserved for a substantial incompatible change or a feature set an end user would notice, and BIGRELEASE for a marketed milestone.
- `publish.yml` calls `ci.yml` as a reusable workflow (`uses: ./.github/workflows/ci.yml`) so the full CI suite runs against the exact SHA it publishes; both the npm release job and the PyPI job depend on that gate, so keep them both wired to it if you touch the release path.
- The `production` GitHub environment (used by both the npm `release` and `publish-pypi` jobs) has a required reviewer and a branch policy limited to `main` configured directly on the environment - this isn't visible in-repo, only via `gh api repos/Teslemetry/tesla-protocol/environments/production`. Recovery/idempotency behavior for an interrupted publish is documented in the README's Releases section - re-running a failed job is always the recovery path, never a manual registry fix.
- Sharp edge: protoletariat is installed with `pip --no-deps` (see `scripts/generate.sh`) because it over-pins `protobuf<6` while only parsing descriptor sets; its `--exclude-imports-glob` patterns match dependency names WITHOUT the `.proto` suffix.
- The codegen toolchain is fully pinned (`scripts/requirements.txt`, ts-proto in root `package.json`). Changing any pin changes generated output and is a coordinated, changeset-worthy change. The `grpcio-tools` pin is load-bearing beyond determinism: its bundled protoc stamps a gencode version into every `_pb2.py`, and protobuf's only compatibility rule is that the *runtime* must not be older than that stamp - there is no minimum-gencode check, so 6.x gencode loads fine on a 7.x runtime (Python gencode back to 3.20 is supported through at least 8.x; see https://protobuf.dev/support/cross-version-runtime-guarantee). That asymmetry is why one wheel serves every runtime and `pyproject.toml` can declare `protobuf>=6.32.0,<8` - do NOT split the package by protoc version. The exact mapping, verified by compiling a throwaway proto with each: grpcio-tools 1.74.0-1.80.0 emit gencode 6.31.1, 1.81.0-1.81.1 emit 6.33.5, 1.82.0+ emit 7.35.x - no release emits 6.32.x. 7.x gencode raises `VersionError` at import for anyone on a 6.x runtime - including Home Assistant, which constrains `protobuf==6.33.6` in `package_constraints.txt` - so advancing grpcio-tools past the 6.x-protoc line is a deliberate drop of 6.x runtime support, not a routine bump. Keep the stamp as LOW as the toolchain allows: it is a minimum runtime requirement, not a compatibility target, so raising it only narrows the range. The declared floor (6.32.0) sits one minor below protobuf's supported window - releasing a minor immediately ends support for the previous one, so 6.33 is the lowest supported 6.x in 2026 (https://protobuf.dev/support/version-support) - which is deliberate margin, not an oversight: do not "fix" it by lowering the floor onto the unpatched 6.31 line. Moving the floor up to the supported window instead is a coordinated floor + toolchain bump, since 6.33.5 is the aligned stamp and grpcio-tools 1.81.1 the pin that emits it - still short of 1.82.0. `ci.yml`'s `python-runtime` matrix runs `scripts/check_gencode_runtime.py` (imports every generated module, so each file's version check actually executes) plus the fixture suite against the declared floor, Home Assistant's pin, and the newest major, so this fails in CI rather than in a downstream install.
- `CarServer.Response.response_msg` (`proto/command/car_server.proto`) must have every field either modelled in the oneof or `reserved` with a reason comment; `scripts/check_reply_coverage.py` (run in CI) enforces this against its known-field set. Before adding a new reply oneof field, check whether its payload type pulls in an app-only namespace - either model it or add it to `KNOWN_REPLY_FIELDS` as a reserved entry.
- Un-reserving a field to model it trips buf's `RESERVED_MESSAGE_NO_DELETE` breaking check even though it's a wire-compatible addition; `buf.yaml` excepts that rule repo-wide.
- `proto/charging/` has no BLE or Fleet API wire path but is published anyway because it describes the vehicle-charging product domain; treat this as a narrow, deliberate exception, not precedent for publishing other app-only namespaces.
- Each `proto/<group>/` compiles as an isolated `--proto_path` root (see `GROUPS_LIST` in `scripts/generate.sh`); there is no cross-group `import`. `proto/energy_device/signed_message.proto` (`Destination`/`KeyIdentity`/`RsaSignatureData`/`SignatureData`/`RoutableMessage`/`Tail`/`Message`) is therefore a self-contained signing envelope for the Powerwall local v1r endpoint, independently defined even though it is wire-shape-identical to `proto/command`'s `UniversalMessage`/`Signatures` vehicle-signing types - the two groups serve different products and neither imports the other, matching the repo's existing pattern of per-group-independent definitions for structurally similar transport primitives (e.g. `SignatureType` is defined separately in both `vcsec.proto` and `signatures.proto`). `RoutableMessage.protobuf_message_as_bytes` carries an opaque serialized `MessageEnvelope` (the domain routing envelope already in `transport.proto`).
- Adding a wholly new proto directory needs registering in three places: the `buf.yaml` module list, `GROUPS_LIST` in `scripts/generate.sh`, and the layout table in `README.md`. TypeScript/Python package exports are wildcarded, so no per-group registration is needed there.
- `scripts/build_catalog.py` regenerates the descriptor-based protobuf surface catalog (`catalog/catalog.json` + `catalog/SUMMARY.md`) from `proto/`: every group, file, package, message, enum, field/tag/type, oneof, reservation, import, and generated TS/Python target. CI fails on any diff or on inconsistent group registration across `buf.yaml`/`GROUPS_LIST`/README, so run it after any proto or group-registration change. `energy_device/google/rpc/status.proto` is catalogued but excluded from message/enum/field totals since it's never a compiled top-level input (only reachable via import).
- `scripts/test_build_catalog.py::test_baseline_totals` asserts literal message/enum/field/file/group counts against the catalog - it's deliberate drift detection, not a frozen spec (see the "Update on purpose" comment beside it). Any proto change that shifts those counts fails CI once; the fix is to update the asserted numbers in the same change, not to treat the failure as a bug.
- Some `energy_device.v1` services (`SMAPI`, `WMSAAPI`) have no modeled request/response bodies, only the routing enum. A few `energy_device.v1` messages carry a field whose type lives in a sibling, not-yet-published package (`energy_registration.v1`) - left unmodeled with a short inline comment rather than guessed at or vendored in.
- `buf breaking`'s tag-renumbering forbidding (see `.changeset/README.md`) is a hard CI gate, not a guideline: it fires even when you have hard evidence an already-published field's tag is wrong, and reserving just the number isn't enough - `FIELD_NO_DELETE` also requires reserving the field's name, which then collides with reusing that name at the corrected number. Don't attempt an in-place renumber; that always needs a maintainer decision. The sanctioned fix (once approved) is reserve the old number and name, then add a differently-named field at the correct number - a breaking rename, not a wire-compatible change, so it needs a major changeset. `NavigationSuperchargerRequest` is a live example: `"order"` stays reserved by name, with `id` at tag 1 and `remote_nav_trip_order` at tag 2.
- Cross-package wire-format confirmation lives in `fixtures/golden/*.json`: language-neutral hex-encoded cases, each consumed by both `packages/typescript/test/*.test.ts` (`node --test`) and `packages/python/tests/test_*.py` (`unittest`), asserting both encoders produce identical bytes and both decoders round-trip them. CI runs both suites. Add a new fixture file per confirmed command/message rather than growing one file unboundedly.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
