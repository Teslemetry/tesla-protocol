// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: vehicle_alert.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp.js";

export const protobufPackage = "telemetry.vehicle_alerts";

/**
 * UPSTREAM-SOURCE: teslamotors/fleet-telemetry
 * UPSTREAM-PATH:   protos/vehicle_alert.proto
 * UPSTREAM-COMMIT: see upstream.json
 * This file tracks upstream verbatim; no Teslemetry extensions.
 */

/** Audience the target audience for the alert. */
export enum Audience {
  Unknown = 0,
  Customer = 1,
  Service = 2,
  ServiceFix = 3,
  UNRECOGNIZED = -1,
}

export function audienceFromJSON(object: any): Audience {
  switch (object) {
    case 0:
    case "Unknown":
      return Audience.Unknown;
    case 1:
    case "Customer":
      return Audience.Customer;
    case 2:
    case "Service":
      return Audience.Service;
    case 3:
    case "ServiceFix":
      return Audience.ServiceFix;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Audience.UNRECOGNIZED;
  }
}

export function audienceToJSON(object: Audience): string {
  switch (object) {
    case Audience.Unknown:
      return "Unknown";
    case Audience.Customer:
      return "Customer";
    case Audience.Service:
      return "Service";
    case Audience.ServiceFix:
      return "ServiceFix";
    case Audience.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** VehicleAlerts is a collection of recent alerts. */
export interface VehicleAlerts {
  alerts: VehicleAlert[];
  createdAt: Date | undefined;
  vin: string;
}

/** VehicleAlert is a single alert and is active if ended_at is nil. */
export interface VehicleAlert {
  name: string;
  audiences: Audience[];
  startedAt: Date | undefined;
  endedAt: Date | undefined;
}

function createBaseVehicleAlerts(): VehicleAlerts {
  return { alerts: [], createdAt: undefined, vin: "" };
}

export const VehicleAlerts: MessageFns<VehicleAlerts> = {
  encode(message: VehicleAlerts, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.alerts) {
      VehicleAlert.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.vin !== "") {
      writer.uint32(26).string(message.vin);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VehicleAlerts {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVehicleAlerts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.alerts.push(VehicleAlert.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vin = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VehicleAlerts {
    return {
      alerts: globalThis.Array.isArray(object?.alerts) ? object.alerts.map((e: any) => VehicleAlert.fromJSON(e)) : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      vin: isSet(object.vin) ? globalThis.String(object.vin) : "",
    };
  },

  toJSON(message: VehicleAlerts): unknown {
    const obj: any = {};
    if (message.alerts?.length) {
      obj.alerts = message.alerts.map((e) => VehicleAlert.toJSON(e));
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.vin !== undefined) {
      obj.vin = message.vin;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VehicleAlerts>, I>>(base?: I): VehicleAlerts {
    return VehicleAlerts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VehicleAlerts>, I>>(object: I): VehicleAlerts {
    const message = createBaseVehicleAlerts();
    message.alerts = object.alerts?.map((e) => VehicleAlert.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.vin = object.vin ?? "";
    return message;
  },
};

function createBaseVehicleAlert(): VehicleAlert {
  return { name: "", audiences: [], startedAt: undefined, endedAt: undefined };
}

export const VehicleAlert: MessageFns<VehicleAlert> = {
  encode(message: VehicleAlert, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    writer.uint32(18).fork();
    for (const v of message.audiences) {
      writer.int32(v);
    }
    writer.join();
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(26).fork()).join();
    }
    if (message.endedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VehicleAlert {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVehicleAlert();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.audiences.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.audiences.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VehicleAlert {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      audiences: globalThis.Array.isArray(object?.audiences)
        ? object.audiences.map((e: any) => audienceFromJSON(e))
        : [],
      startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined,
      endedAt: isSet(object.endedAt) ? fromJsonTimestamp(object.endedAt) : undefined,
    };
  },

  toJSON(message: VehicleAlert): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.audiences?.length) {
      obj.audiences = message.audiences.map((e) => audienceToJSON(e));
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.endedAt !== undefined) {
      obj.endedAt = message.endedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VehicleAlert>, I>>(base?: I): VehicleAlert {
    return VehicleAlert.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VehicleAlert>, I>>(object: I): VehicleAlert {
    const message = createBaseVehicleAlert();
    message.name = object.name ?? "";
    message.audiences = object.audiences?.map((e) => e) || [];
    message.startedAt = object.startedAt ?? undefined;
    message.endedAt = object.endedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
