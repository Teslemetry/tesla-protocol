// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: device.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tesla.proto.energy_device.v1";

/**
 * TESLEMETRY SOURCE-OF-RECORD - no upstream.
 * Provenance: Tesla Energy gateway (TEG) local API, from our own
 * observations and contributions from the community
 */

export enum DeviceType {
  DEVICE_TYPE_INVALID = 0,
  DEVICE_TYPE_GEN3WC = 1,
  DEVICE_TYPE_PVCOM = 2,
  DEVICE_TYPE_MSA = 3,
  DEVICE_TYPE_SITECONTROLLER = 4,
  DEVICE_TYPE_MEGAPUTER = 5,
  DEVICE_TYPE_IPM = 6,
  UNRECOGNIZED = -1,
}

export function deviceTypeFromJSON(object: any): DeviceType {
  switch (object) {
    case 0:
    case "DEVICE_TYPE_INVALID":
      return DeviceType.DEVICE_TYPE_INVALID;
    case 1:
    case "DEVICE_TYPE_GEN3WC":
      return DeviceType.DEVICE_TYPE_GEN3WC;
    case 2:
    case "DEVICE_TYPE_PVCOM":
      return DeviceType.DEVICE_TYPE_PVCOM;
    case 3:
    case "DEVICE_TYPE_MSA":
      return DeviceType.DEVICE_TYPE_MSA;
    case 4:
    case "DEVICE_TYPE_SITECONTROLLER":
      return DeviceType.DEVICE_TYPE_SITECONTROLLER;
    case 5:
    case "DEVICE_TYPE_MEGAPUTER":
      return DeviceType.DEVICE_TYPE_MEGAPUTER;
    case 6:
    case "DEVICE_TYPE_IPM":
      return DeviceType.DEVICE_TYPE_IPM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceType.UNRECOGNIZED;
  }
}

export function deviceTypeToJSON(object: DeviceType): string {
  switch (object) {
    case DeviceType.DEVICE_TYPE_INVALID:
      return "DEVICE_TYPE_INVALID";
    case DeviceType.DEVICE_TYPE_GEN3WC:
      return "DEVICE_TYPE_GEN3WC";
    case DeviceType.DEVICE_TYPE_PVCOM:
      return "DEVICE_TYPE_PVCOM";
    case DeviceType.DEVICE_TYPE_MSA:
      return "DEVICE_TYPE_MSA";
    case DeviceType.DEVICE_TYPE_SITECONTROLLER:
      return "DEVICE_TYPE_SITECONTROLLER";
    case DeviceType.DEVICE_TYPE_MEGAPUTER:
      return "DEVICE_TYPE_MEGAPUTER";
    case DeviceType.DEVICE_TYPE_IPM:
      return "DEVICE_TYPE_IPM";
    case DeviceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DeviceCertFormat {
  DEVICE_CERT_FORMAT_INVALID = 0,
  DEVICE_CERT_FORMAT_DER = 1,
  DEVICE_CERT_FORMAT_PEM = 2,
  UNRECOGNIZED = -1,
}

export function deviceCertFormatFromJSON(object: any): DeviceCertFormat {
  switch (object) {
    case 0:
    case "DEVICE_CERT_FORMAT_INVALID":
      return DeviceCertFormat.DEVICE_CERT_FORMAT_INVALID;
    case 1:
    case "DEVICE_CERT_FORMAT_DER":
      return DeviceCertFormat.DEVICE_CERT_FORMAT_DER;
    case 2:
    case "DEVICE_CERT_FORMAT_PEM":
      return DeviceCertFormat.DEVICE_CERT_FORMAT_PEM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceCertFormat.UNRECOGNIZED;
  }
}

export function deviceCertFormatToJSON(object: DeviceCertFormat): string {
  switch (object) {
    case DeviceCertFormat.DEVICE_CERT_FORMAT_INVALID:
      return "DEVICE_CERT_FORMAT_INVALID";
    case DeviceCertFormat.DEVICE_CERT_FORMAT_DER:
      return "DEVICE_CERT_FORMAT_DER";
    case DeviceCertFormat.DEVICE_CERT_FORMAT_PEM:
      return "DEVICE_CERT_FORMAT_PEM";
    case DeviceCertFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DeviceSignatureType {
  DEVICE_SIGNATURE_TYPE_INVALID = 0,
  DEVICE_SIGNATURE_TYPE_SHA256_ECDSA_ASN1 = 1,
  UNRECOGNIZED = -1,
}

export function deviceSignatureTypeFromJSON(object: any): DeviceSignatureType {
  switch (object) {
    case 0:
    case "DEVICE_SIGNATURE_TYPE_INVALID":
      return DeviceSignatureType.DEVICE_SIGNATURE_TYPE_INVALID;
    case 1:
    case "DEVICE_SIGNATURE_TYPE_SHA256_ECDSA_ASN1":
      return DeviceSignatureType.DEVICE_SIGNATURE_TYPE_SHA256_ECDSA_ASN1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceSignatureType.UNRECOGNIZED;
  }
}

export function deviceSignatureTypeToJSON(object: DeviceSignatureType): string {
  switch (object) {
    case DeviceSignatureType.DEVICE_SIGNATURE_TYPE_INVALID:
      return "DEVICE_SIGNATURE_TYPE_INVALID";
    case DeviceSignatureType.DEVICE_SIGNATURE_TYPE_SHA256_ECDSA_ASN1:
      return "DEVICE_SIGNATURE_TYPE_SHA256_ECDSA_ASN1";
    case DeviceSignatureType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Cipher {
  CIPHER_INVALID = 0,
  CIPHER_RSA_OAEP_SHA256 = 2,
  UNRECOGNIZED = -1,
}

export function cipherFromJSON(object: any): Cipher {
  switch (object) {
    case 0:
    case "CIPHER_INVALID":
      return Cipher.CIPHER_INVALID;
    case 2:
    case "CIPHER_RSA_OAEP_SHA256":
      return Cipher.CIPHER_RSA_OAEP_SHA256;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Cipher.UNRECOGNIZED;
  }
}

export function cipherToJSON(object: Cipher): string {
  switch (object) {
    case Cipher.CIPHER_INVALID:
      return "CIPHER_INVALID";
    case Cipher.CIPHER_RSA_OAEP_SHA256:
      return "CIPHER_RSA_OAEP_SHA256";
    case Cipher.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum TEGDeviceType {
  TEG_DEVICE_TYPE_INVALID = 0,
  TEG_DEVICE_TYPE_SMC = 1,
  TEG_DEVICE_TYPE_HEC_GATEWAY1 = 2,
  TEG_DEVICE_TYPE_TEG_GATEWAY2 = 3,
  TEG_DEVICE_TYPE_TEG_POWERWALLPLUS = 4,
  TEG_DEVICE_TYPE_TEG_MP1 = 5,
  TEG_DEVICE_TYPE_TEG_SUPERCHARGER = 6,
  TEG_DEVICE_TYPE_TACO_POWERWALL3 = 7,
  TEG_DEVICE_TYPE_TEG_PVI = 8,
  TEG_DEVICE_TYPE_TACO_GATEWAY3V = 9,
  TEG_DEVICE_TYPE_TACO_WALLBOX = 10,
  UNRECOGNIZED = -1,
}

export function tEGDeviceTypeFromJSON(object: any): TEGDeviceType {
  switch (object) {
    case 0:
    case "TEG_DEVICE_TYPE_INVALID":
      return TEGDeviceType.TEG_DEVICE_TYPE_INVALID;
    case 1:
    case "TEG_DEVICE_TYPE_SMC":
      return TEGDeviceType.TEG_DEVICE_TYPE_SMC;
    case 2:
    case "TEG_DEVICE_TYPE_HEC_GATEWAY1":
      return TEGDeviceType.TEG_DEVICE_TYPE_HEC_GATEWAY1;
    case 3:
    case "TEG_DEVICE_TYPE_TEG_GATEWAY2":
      return TEGDeviceType.TEG_DEVICE_TYPE_TEG_GATEWAY2;
    case 4:
    case "TEG_DEVICE_TYPE_TEG_POWERWALLPLUS":
      return TEGDeviceType.TEG_DEVICE_TYPE_TEG_POWERWALLPLUS;
    case 5:
    case "TEG_DEVICE_TYPE_TEG_MP1":
      return TEGDeviceType.TEG_DEVICE_TYPE_TEG_MP1;
    case 6:
    case "TEG_DEVICE_TYPE_TEG_SUPERCHARGER":
      return TEGDeviceType.TEG_DEVICE_TYPE_TEG_SUPERCHARGER;
    case 7:
    case "TEG_DEVICE_TYPE_TACO_POWERWALL3":
      return TEGDeviceType.TEG_DEVICE_TYPE_TACO_POWERWALL3;
    case 8:
    case "TEG_DEVICE_TYPE_TEG_PVI":
      return TEGDeviceType.TEG_DEVICE_TYPE_TEG_PVI;
    case 9:
    case "TEG_DEVICE_TYPE_TACO_GATEWAY3V":
      return TEGDeviceType.TEG_DEVICE_TYPE_TACO_GATEWAY3V;
    case 10:
    case "TEG_DEVICE_TYPE_TACO_WALLBOX":
      return TEGDeviceType.TEG_DEVICE_TYPE_TACO_WALLBOX;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TEGDeviceType.UNRECOGNIZED;
  }
}

export function tEGDeviceTypeToJSON(object: TEGDeviceType): string {
  switch (object) {
    case TEGDeviceType.TEG_DEVICE_TYPE_INVALID:
      return "TEG_DEVICE_TYPE_INVALID";
    case TEGDeviceType.TEG_DEVICE_TYPE_SMC:
      return "TEG_DEVICE_TYPE_SMC";
    case TEGDeviceType.TEG_DEVICE_TYPE_HEC_GATEWAY1:
      return "TEG_DEVICE_TYPE_HEC_GATEWAY1";
    case TEGDeviceType.TEG_DEVICE_TYPE_TEG_GATEWAY2:
      return "TEG_DEVICE_TYPE_TEG_GATEWAY2";
    case TEGDeviceType.TEG_DEVICE_TYPE_TEG_POWERWALLPLUS:
      return "TEG_DEVICE_TYPE_TEG_POWERWALLPLUS";
    case TEGDeviceType.TEG_DEVICE_TYPE_TEG_MP1:
      return "TEG_DEVICE_TYPE_TEG_MP1";
    case TEGDeviceType.TEG_DEVICE_TYPE_TEG_SUPERCHARGER:
      return "TEG_DEVICE_TYPE_TEG_SUPERCHARGER";
    case TEGDeviceType.TEG_DEVICE_TYPE_TACO_POWERWALL3:
      return "TEG_DEVICE_TYPE_TACO_POWERWALL3";
    case TEGDeviceType.TEG_DEVICE_TYPE_TEG_PVI:
      return "TEG_DEVICE_TYPE_TEG_PVI";
    case TEGDeviceType.TEG_DEVICE_TYPE_TACO_GATEWAY3V:
      return "TEG_DEVICE_TYPE_TACO_GATEWAY3V";
    case TEGDeviceType.TEG_DEVICE_TYPE_TACO_WALLBOX:
      return "TEG_DEVICE_TYPE_TACO_WALLBOX";
    case TEGDeviceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum iPMDeviceType {
  IPM_DEVICE_TYPE_INVALID = 0,
  IPM_DEVICE_TYPE_POWERPACK3 = 1,
  IPM_DEVICE_TYPE_SUPERCHARGER4 = 2,
  UNRECOGNIZED = -1,
}

export function iPMDeviceTypeFromJSON(object: any): iPMDeviceType {
  switch (object) {
    case 0:
    case "IPM_DEVICE_TYPE_INVALID":
      return iPMDeviceType.IPM_DEVICE_TYPE_INVALID;
    case 1:
    case "IPM_DEVICE_TYPE_POWERPACK3":
      return iPMDeviceType.IPM_DEVICE_TYPE_POWERPACK3;
    case 2:
    case "IPM_DEVICE_TYPE_SUPERCHARGER4":
      return iPMDeviceType.IPM_DEVICE_TYPE_SUPERCHARGER4;
    case -1:
    case "UNRECOGNIZED":
    default:
      return iPMDeviceType.UNRECOGNIZED;
  }
}

export function iPMDeviceTypeToJSON(object: iPMDeviceType): string {
  switch (object) {
    case iPMDeviceType.IPM_DEVICE_TYPE_INVALID:
      return "IPM_DEVICE_TYPE_INVALID";
    case iPMDeviceType.IPM_DEVICE_TYPE_POWERPACK3:
      return "IPM_DEVICE_TYPE_POWERPACK3";
    case iPMDeviceType.IPM_DEVICE_TYPE_SUPERCHARGER4:
      return "IPM_DEVICE_TYPE_SUPERCHARGER4";
    case iPMDeviceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Din {
  value: string;
}

export interface EcuId {
  partNumber: string;
  serialNumber: string;
}

export interface DeviceSignedPayload {
  payload: Uint8Array;
  deviceSignatureType: DeviceSignatureType;
  signature: Uint8Array;
  deviceCertFormat: DeviceCertFormat;
  deviceCert: Uint8Array;
}

export interface EncryptedMessage {
  cipher: Cipher;
  cipherText: Uint8Array;
}

function createBaseDin(): Din {
  return { value: "" };
}

export const Din: MessageFns<Din> = {
  encode(message: Din, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Din {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Din {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: Din): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Din>, I>>(base?: I): Din {
    return Din.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Din>, I>>(object: I): Din {
    const message = createBaseDin();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEcuId(): EcuId {
  return { partNumber: "", serialNumber: "" };
}

export const EcuId: MessageFns<EcuId> = {
  encode(message: EcuId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.partNumber !== "") {
      writer.uint32(10).string(message.partNumber);
    }
    if (message.serialNumber !== "") {
      writer.uint32(18).string(message.serialNumber);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EcuId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEcuId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.partNumber = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serialNumber = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EcuId {
    return {
      partNumber: isSet(object.partNumber) ? globalThis.String(object.partNumber) : "",
      serialNumber: isSet(object.serialNumber) ? globalThis.String(object.serialNumber) : "",
    };
  },

  toJSON(message: EcuId): unknown {
    const obj: any = {};
    if (message.partNumber !== undefined) {
      obj.partNumber = message.partNumber;
    }
    if (message.serialNumber !== undefined) {
      obj.serialNumber = message.serialNumber;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EcuId>, I>>(base?: I): EcuId {
    return EcuId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EcuId>, I>>(object: I): EcuId {
    const message = createBaseEcuId();
    message.partNumber = object.partNumber ?? "";
    message.serialNumber = object.serialNumber ?? "";
    return message;
  },
};

function createBaseDeviceSignedPayload(): DeviceSignedPayload {
  return {
    payload: new Uint8Array(0),
    deviceSignatureType: 0,
    signature: new Uint8Array(0),
    deviceCertFormat: 0,
    deviceCert: new Uint8Array(0),
  };
}

export const DeviceSignedPayload: MessageFns<DeviceSignedPayload> = {
  encode(message: DeviceSignedPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.deviceSignatureType !== 0) {
      writer.uint32(16).int32(message.deviceSignatureType);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    if (message.deviceCertFormat !== 0) {
      writer.uint32(32).int32(message.deviceCertFormat);
    }
    if (message.deviceCert.length !== 0) {
      writer.uint32(42).bytes(message.deviceCert);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceSignedPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceSignedPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deviceSignatureType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deviceCertFormat = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deviceCert = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceSignedPayload {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      deviceSignatureType: isSet(object.deviceSignatureType)
        ? deviceSignatureTypeFromJSON(object.deviceSignatureType)
        : 0,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      deviceCertFormat: isSet(object.deviceCertFormat) ? deviceCertFormatFromJSON(object.deviceCertFormat) : 0,
      deviceCert: isSet(object.deviceCert) ? bytesFromBase64(object.deviceCert) : new Uint8Array(0),
    };
  },

  toJSON(message: DeviceSignedPayload): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.deviceSignatureType !== undefined) {
      obj.deviceSignatureType = deviceSignatureTypeToJSON(message.deviceSignatureType);
    }
    if (message.signature !== undefined) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.deviceCertFormat !== undefined) {
      obj.deviceCertFormat = deviceCertFormatToJSON(message.deviceCertFormat);
    }
    if (message.deviceCert !== undefined) {
      obj.deviceCert = base64FromBytes(message.deviceCert);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceSignedPayload>, I>>(base?: I): DeviceSignedPayload {
    return DeviceSignedPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceSignedPayload>, I>>(object: I): DeviceSignedPayload {
    const message = createBaseDeviceSignedPayload();
    message.payload = object.payload ?? new Uint8Array(0);
    message.deviceSignatureType = object.deviceSignatureType ?? 0;
    message.signature = object.signature ?? new Uint8Array(0);
    message.deviceCertFormat = object.deviceCertFormat ?? 0;
    message.deviceCert = object.deviceCert ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEncryptedMessage(): EncryptedMessage {
  return { cipher: 0, cipherText: new Uint8Array(0) };
}

export const EncryptedMessage: MessageFns<EncryptedMessage> = {
  encode(message: EncryptedMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cipher !== 0) {
      writer.uint32(8).int32(message.cipher);
    }
    if (message.cipherText.length !== 0) {
      writer.uint32(18).bytes(message.cipherText);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EncryptedMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEncryptedMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cipher = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cipherText = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EncryptedMessage {
    return {
      cipher: isSet(object.cipher) ? cipherFromJSON(object.cipher) : 0,
      cipherText: isSet(object.cipherText) ? bytesFromBase64(object.cipherText) : new Uint8Array(0),
    };
  },

  toJSON(message: EncryptedMessage): unknown {
    const obj: any = {};
    if (message.cipher !== undefined) {
      obj.cipher = cipherToJSON(message.cipher);
    }
    if (message.cipherText !== undefined) {
      obj.cipherText = base64FromBytes(message.cipherText);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EncryptedMessage>, I>>(base?: I): EncryptedMessage {
    return EncryptedMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EncryptedMessage>, I>>(object: I): EncryptedMessage {
    const message = createBaseEncryptedMessage();
    message.cipher = object.cipher ?? 0;
    message.cipherText = object.cipherText ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
