// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: authorization_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp.js";

export const protobufPackage = "tesla.proto.energy_device.v1";

/**
 * TESLEMETRY SOURCE-OF-RECORD - no upstream.
 * Provenance: Tesla Energy gateway (TEG) local API, from our own
 * observations and contributions from the community
 */

export enum AuthorizationRole {
  AUTHORIZATION_ROLE_INVALID = 0,
  AUTHORIZATION_ROLE_CUSTOMER = 1,
  AUTHORIZATION_ROLE_VEHICLE = 2,
  UNRECOGNIZED = -1,
}

export function authorizationRoleFromJSON(object: any): AuthorizationRole {
  switch (object) {
    case 0:
    case "AUTHORIZATION_ROLE_INVALID":
      return AuthorizationRole.AUTHORIZATION_ROLE_INVALID;
    case 1:
    case "AUTHORIZATION_ROLE_CUSTOMER":
      return AuthorizationRole.AUTHORIZATION_ROLE_CUSTOMER;
    case 2:
    case "AUTHORIZATION_ROLE_VEHICLE":
      return AuthorizationRole.AUTHORIZATION_ROLE_VEHICLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthorizationRole.UNRECOGNIZED;
  }
}

export function authorizationRoleToJSON(object: AuthorizationRole): string {
  switch (object) {
    case AuthorizationRole.AUTHORIZATION_ROLE_INVALID:
      return "AUTHORIZATION_ROLE_INVALID";
    case AuthorizationRole.AUTHORIZATION_ROLE_CUSTOMER:
      return "AUTHORIZATION_ROLE_CUSTOMER";
    case AuthorizationRole.AUTHORIZATION_ROLE_VEHICLE:
      return "AUTHORIZATION_ROLE_VEHICLE";
    case AuthorizationRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AuthorizedClientType {
  AUTHORIZED_CLIENT_TYPE_INVALID = 0,
  AUTHORIZED_CLIENT_TYPE_CUSTOMER_MOBILE_APP = 1,
  AUTHORIZED_CLIENT_TYPE_VEHICLE = 2,
  UNRECOGNIZED = -1,
}

export function authorizedClientTypeFromJSON(object: any): AuthorizedClientType {
  switch (object) {
    case 0:
    case "AUTHORIZED_CLIENT_TYPE_INVALID":
      return AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_INVALID;
    case 1:
    case "AUTHORIZED_CLIENT_TYPE_CUSTOMER_MOBILE_APP":
      return AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_CUSTOMER_MOBILE_APP;
    case 2:
    case "AUTHORIZED_CLIENT_TYPE_VEHICLE":
      return AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_VEHICLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthorizedClientType.UNRECOGNIZED;
  }
}

export function authorizedClientTypeToJSON(object: AuthorizedClientType): string {
  switch (object) {
    case AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_INVALID:
      return "AUTHORIZED_CLIENT_TYPE_INVALID";
    case AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_CUSTOMER_MOBILE_APP:
      return "AUTHORIZED_CLIENT_TYPE_CUSTOMER_MOBILE_APP";
    case AuthorizedClientType.AUTHORIZED_CLIENT_TYPE_VEHICLE:
      return "AUTHORIZED_CLIENT_TYPE_VEHICLE";
    case AuthorizedClientType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AuthorizedKeyType {
  AUTHORIZED_KEY_TYPE_INVALID = 0,
  AUTHORIZED_KEY_TYPE_RSA = 1,
  AUTHORIZED_KEY_TYPE_ECC = 2,
  UNRECOGNIZED = -1,
}

export function authorizedKeyTypeFromJSON(object: any): AuthorizedKeyType {
  switch (object) {
    case 0:
    case "AUTHORIZED_KEY_TYPE_INVALID":
      return AuthorizedKeyType.AUTHORIZED_KEY_TYPE_INVALID;
    case 1:
    case "AUTHORIZED_KEY_TYPE_RSA":
      return AuthorizedKeyType.AUTHORIZED_KEY_TYPE_RSA;
    case 2:
    case "AUTHORIZED_KEY_TYPE_ECC":
      return AuthorizedKeyType.AUTHORIZED_KEY_TYPE_ECC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthorizedKeyType.UNRECOGNIZED;
  }
}

export function authorizedKeyTypeToJSON(object: AuthorizedKeyType): string {
  switch (object) {
    case AuthorizedKeyType.AUTHORIZED_KEY_TYPE_INVALID:
      return "AUTHORIZED_KEY_TYPE_INVALID";
    case AuthorizedKeyType.AUTHORIZED_KEY_TYPE_RSA:
      return "AUTHORIZED_KEY_TYPE_RSA";
    case AuthorizedKeyType.AUTHORIZED_KEY_TYPE_ECC:
      return "AUTHORIZED_KEY_TYPE_ECC";
    case AuthorizedKeyType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AuthorizedState {
  AUTHORIZED_STATE_INVALID = 0,
  AUTHORIZED_STATE_PENDING_VERIFICATION = 1,
  AUTHORIZED_STATE_PENDING_VERIFICATION_TIMEOUT = 2,
  AUTHORIZED_STATE_VERIFIED = 3,
  AUTHORIZED_STATE_REMOVED = 4,
  UNRECOGNIZED = -1,
}

export function authorizedStateFromJSON(object: any): AuthorizedState {
  switch (object) {
    case 0:
    case "AUTHORIZED_STATE_INVALID":
      return AuthorizedState.AUTHORIZED_STATE_INVALID;
    case 1:
    case "AUTHORIZED_STATE_PENDING_VERIFICATION":
      return AuthorizedState.AUTHORIZED_STATE_PENDING_VERIFICATION;
    case 2:
    case "AUTHORIZED_STATE_PENDING_VERIFICATION_TIMEOUT":
      return AuthorizedState.AUTHORIZED_STATE_PENDING_VERIFICATION_TIMEOUT;
    case 3:
    case "AUTHORIZED_STATE_VERIFIED":
      return AuthorizedState.AUTHORIZED_STATE_VERIFIED;
    case 4:
    case "AUTHORIZED_STATE_REMOVED":
      return AuthorizedState.AUTHORIZED_STATE_REMOVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthorizedState.UNRECOGNIZED;
  }
}

export function authorizedStateToJSON(object: AuthorizedState): string {
  switch (object) {
    case AuthorizedState.AUTHORIZED_STATE_INVALID:
      return "AUTHORIZED_STATE_INVALID";
    case AuthorizedState.AUTHORIZED_STATE_PENDING_VERIFICATION:
      return "AUTHORIZED_STATE_PENDING_VERIFICATION";
    case AuthorizedState.AUTHORIZED_STATE_PENDING_VERIFICATION_TIMEOUT:
      return "AUTHORIZED_STATE_PENDING_VERIFICATION_TIMEOUT";
    case AuthorizedState.AUTHORIZED_STATE_VERIFIED:
      return "AUTHORIZED_STATE_VERIFIED";
    case AuthorizedState.AUTHORIZED_STATE_REMOVED:
      return "AUTHORIZED_STATE_REMOVED";
    case AuthorizedState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AuthorizedVerificationType {
  AUTHORIZED_VERIFICATION_TYPE_INVALID = 0,
  AUTHORIZED_VERIFICATION_TYPE_PRESENCE_PROOF = 1,
  AUTHORIZED_VERIFICATION_TYPE_BLE = 2,
  AUTHORIZED_VERIFICATION_TYPE_SIGNED = 3,
  AUTHORIZED_VERIFICATION_TYPE_HERMES_COMMAND = 4,
  UNRECOGNIZED = -1,
}

export function authorizedVerificationTypeFromJSON(object: any): AuthorizedVerificationType {
  switch (object) {
    case 0:
    case "AUTHORIZED_VERIFICATION_TYPE_INVALID":
      return AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_INVALID;
    case 1:
    case "AUTHORIZED_VERIFICATION_TYPE_PRESENCE_PROOF":
      return AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_PRESENCE_PROOF;
    case 2:
    case "AUTHORIZED_VERIFICATION_TYPE_BLE":
      return AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_BLE;
    case 3:
    case "AUTHORIZED_VERIFICATION_TYPE_SIGNED":
      return AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_SIGNED;
    case 4:
    case "AUTHORIZED_VERIFICATION_TYPE_HERMES_COMMAND":
      return AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_HERMES_COMMAND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthorizedVerificationType.UNRECOGNIZED;
  }
}

export function authorizedVerificationTypeToJSON(object: AuthorizedVerificationType): string {
  switch (object) {
    case AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_INVALID:
      return "AUTHORIZED_VERIFICATION_TYPE_INVALID";
    case AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_PRESENCE_PROOF:
      return "AUTHORIZED_VERIFICATION_TYPE_PRESENCE_PROOF";
    case AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_BLE:
      return "AUTHORIZED_VERIFICATION_TYPE_BLE";
    case AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_SIGNED:
      return "AUTHORIZED_VERIFICATION_TYPE_SIGNED";
    case AuthorizedVerificationType.AUTHORIZED_VERIFICATION_TYPE_HERMES_COMMAND:
      return "AUTHORIZED_VERIFICATION_TYPE_HERMES_COMMAND";
    case AuthorizedVerificationType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AuthorizationRecord {
  type: AuthorizedClientType;
  description: string;
  keyType: AuthorizedKeyType;
  publicKey: Uint8Array;
  roles: AuthorizationRole[];
  state: AuthorizedState;
  verification: AuthorizedVerificationType;
  addedTime: Date | undefined;
  identifier?: string | undefined;
  authorizedByPublicKey?: Uint8Array | undefined;
}

function createBaseAuthorizationRecord(): AuthorizationRecord {
  return {
    type: 0,
    description: "",
    keyType: 0,
    publicKey: new Uint8Array(0),
    roles: [],
    state: 0,
    verification: 0,
    addedTime: undefined,
    identifier: undefined,
    authorizedByPublicKey: undefined,
  };
}

export const AuthorizationRecord: MessageFns<AuthorizationRecord> = {
  encode(message: AuthorizationRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.keyType !== 0) {
      writer.uint32(24).int32(message.keyType);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(34).bytes(message.publicKey);
    }
    writer.uint32(42).fork();
    for (const v of message.roles) {
      writer.int32(v);
    }
    writer.join();
    if (message.state !== 0) {
      writer.uint32(48).int32(message.state);
    }
    if (message.verification !== 0) {
      writer.uint32(56).int32(message.verification);
    }
    if (message.addedTime !== undefined) {
      Timestamp.encode(toTimestamp(message.addedTime), writer.uint32(66).fork()).join();
    }
    if (message.identifier !== undefined) {
      writer.uint32(74).string(message.identifier);
    }
    if (message.authorizedByPublicKey !== undefined) {
      writer.uint32(82).bytes(message.authorizedByPublicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizationRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizationRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.keyType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.roles.push(reader.int32() as any);

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.roles.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.verification = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.addedTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.identifier = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.authorizedByPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthorizationRecord {
    return {
      type: isSet(object.type) ? authorizedClientTypeFromJSON(object.type) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      keyType: isSet(object.keyType) ? authorizedKeyTypeFromJSON(object.keyType) : 0,
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => authorizationRoleFromJSON(e)) : [],
      state: isSet(object.state) ? authorizedStateFromJSON(object.state) : 0,
      verification: isSet(object.verification) ? authorizedVerificationTypeFromJSON(object.verification) : 0,
      addedTime: isSet(object.addedTime) ? fromJsonTimestamp(object.addedTime) : undefined,
      identifier: isSet(object.identifier) ? globalThis.String(object.identifier) : undefined,
      authorizedByPublicKey: isSet(object.authorizedByPublicKey)
        ? bytesFromBase64(object.authorizedByPublicKey)
        : undefined,
    };
  },

  toJSON(message: AuthorizationRecord): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = authorizedClientTypeToJSON(message.type);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.keyType !== undefined) {
      obj.keyType = authorizedKeyTypeToJSON(message.keyType);
    }
    if (message.publicKey !== undefined) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.roles?.length) {
      obj.roles = message.roles.map((e) => authorizationRoleToJSON(e));
    }
    if (message.state !== undefined) {
      obj.state = authorizedStateToJSON(message.state);
    }
    if (message.verification !== undefined) {
      obj.verification = authorizedVerificationTypeToJSON(message.verification);
    }
    if (message.addedTime !== undefined) {
      obj.addedTime = message.addedTime.toISOString();
    }
    if (message.identifier !== undefined) {
      obj.identifier = message.identifier;
    }
    if (message.authorizedByPublicKey !== undefined) {
      obj.authorizedByPublicKey = base64FromBytes(message.authorizedByPublicKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthorizationRecord>, I>>(base?: I): AuthorizationRecord {
    return AuthorizationRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthorizationRecord>, I>>(object: I): AuthorizationRecord {
    const message = createBaseAuthorizationRecord();
    message.type = object.type ?? 0;
    message.description = object.description ?? "";
    message.keyType = object.keyType ?? 0;
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.roles = object.roles?.map((e) => e) || [];
    message.state = object.state ?? 0;
    message.verification = object.verification ?? 0;
    message.addedTime = object.addedTime ?? undefined;
    message.identifier = object.identifier ?? undefined;
    message.authorizedByPublicKey = object.authorizedByPublicKey ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
