// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: update.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tesla.proto.energy_device.v1";

/**
 * TESLEMETRY SOURCE-OF-RECORD - no upstream.
 * Provenance: Tesla Energy gateway (TEG) local API, from our own
 * observations and contributions from the community
 */

export enum UpdateStatus {
  UPDATE_STATUS_INVALID = 0,
  UPDATE_STATUS_IDLE = 1,
  UPDATE_STATUS_DOWNLOADING = 2,
  UPDATE_STATUS_DOWNLOADED = 3,
  UPDATE_STATUS_STAGED = 4,
  UNRECOGNIZED = -1,
}

export function updateStatusFromJSON(object: any): UpdateStatus {
  switch (object) {
    case 0:
    case "UPDATE_STATUS_INVALID":
      return UpdateStatus.UPDATE_STATUS_INVALID;
    case 1:
    case "UPDATE_STATUS_IDLE":
      return UpdateStatus.UPDATE_STATUS_IDLE;
    case 2:
    case "UPDATE_STATUS_DOWNLOADING":
      return UpdateStatus.UPDATE_STATUS_DOWNLOADING;
    case 3:
    case "UPDATE_STATUS_DOWNLOADED":
      return UpdateStatus.UPDATE_STATUS_DOWNLOADED;
    case 4:
    case "UPDATE_STATUS_STAGED":
      return UpdateStatus.UPDATE_STATUS_STAGED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UpdateStatus.UNRECOGNIZED;
  }
}

export function updateStatusToJSON(object: UpdateStatus): string {
  switch (object) {
    case UpdateStatus.UPDATE_STATUS_INVALID:
      return "UPDATE_STATUS_INVALID";
    case UpdateStatus.UPDATE_STATUS_IDLE:
      return "UPDATE_STATUS_IDLE";
    case UpdateStatus.UPDATE_STATUS_DOWNLOADING:
      return "UPDATE_STATUS_DOWNLOADING";
    case UpdateStatus.UPDATE_STATUS_DOWNLOADED:
      return "UPDATE_STATUS_DOWNLOADED";
    case UpdateStatus.UPDATE_STATUS_STAGED:
      return "UPDATE_STATUS_STAGED";
    case UpdateStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum UpdateHandshakeResult {
  UPDATE_HANDSHAKE_RESULT_INVALID = 0,
  UPDATE_HANDSHAKE_RESULT_UNDERWAY = 1,
  UPDATE_HANDSHAKE_RESULT_FAILED = 2,
  UPDATE_HANDSHAKE_RESULT_NON_ACTIONABLE = 3,
  UPDATE_HANDSHAKE_RESULT_UPDATE_STAGED = 4,
  UNRECOGNIZED = -1,
}

export function updateHandshakeResultFromJSON(object: any): UpdateHandshakeResult {
  switch (object) {
    case 0:
    case "UPDATE_HANDSHAKE_RESULT_INVALID":
      return UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_INVALID;
    case 1:
    case "UPDATE_HANDSHAKE_RESULT_UNDERWAY":
      return UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_UNDERWAY;
    case 2:
    case "UPDATE_HANDSHAKE_RESULT_FAILED":
      return UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_FAILED;
    case 3:
    case "UPDATE_HANDSHAKE_RESULT_NON_ACTIONABLE":
      return UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_NON_ACTIONABLE;
    case 4:
    case "UPDATE_HANDSHAKE_RESULT_UPDATE_STAGED":
      return UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_UPDATE_STAGED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UpdateHandshakeResult.UNRECOGNIZED;
  }
}

export function updateHandshakeResultToJSON(object: UpdateHandshakeResult): string {
  switch (object) {
    case UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_INVALID:
      return "UPDATE_HANDSHAKE_RESULT_INVALID";
    case UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_UNDERWAY:
      return "UPDATE_HANDSHAKE_RESULT_UNDERWAY";
    case UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_FAILED:
      return "UPDATE_HANDSHAKE_RESULT_FAILED";
    case UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_NON_ACTIONABLE:
      return "UPDATE_HANDSHAKE_RESULT_NON_ACTIONABLE";
    case UpdateHandshakeResult.UPDATE_HANDSHAKE_RESULT_UPDATE_STAGED:
      return "UPDATE_HANDSHAKE_RESULT_UPDATE_STAGED";
    case UpdateHandshakeResult.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum LastUpdateResult {
  LAST_UPDATE_RESULT_INVALID = 0,
  LAST_UPDATE_RESULT_FAILED = 1,
  LAST_UPDATE_RESULT_SUCCEEDED = 2,
  UNRECOGNIZED = -1,
}

export function lastUpdateResultFromJSON(object: any): LastUpdateResult {
  switch (object) {
    case 0:
    case "LAST_UPDATE_RESULT_INVALID":
      return LastUpdateResult.LAST_UPDATE_RESULT_INVALID;
    case 1:
    case "LAST_UPDATE_RESULT_FAILED":
      return LastUpdateResult.LAST_UPDATE_RESULT_FAILED;
    case 2:
    case "LAST_UPDATE_RESULT_SUCCEEDED":
      return LastUpdateResult.LAST_UPDATE_RESULT_SUCCEEDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LastUpdateResult.UNRECOGNIZED;
  }
}

export function lastUpdateResultToJSON(object: LastUpdateResult): string {
  switch (object) {
    case LastUpdateResult.LAST_UPDATE_RESULT_INVALID:
      return "LAST_UPDATE_RESULT_INVALID";
    case LastUpdateResult.LAST_UPDATE_RESULT_FAILED:
      return "LAST_UPDATE_RESULT_FAILED";
    case LastUpdateResult.LAST_UPDATE_RESULT_SUCCEEDED:
      return "LAST_UPDATE_RESULT_SUCCEEDED";
    case LastUpdateResult.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface FirmwareVersion {
  version: string;
  githash: Uint8Array;
}

export interface ServerStagedPackage {
  downloadUrl: string;
  packageId: number;
  packageSignature: Uint8Array;
  serverStagedVersion: FirmwareVersion | undefined;
}

export interface SystemUpdate {
  handshakeResult: number;
  updateStatus: number;
  serverStagedVersion: FirmwareVersion | undefined;
  totalBytes: number;
  bytesOffset: number;
  estimatedBytesPerSecond: number;
  lastHandshakeTimestamp: number;
  lastUpdateResult: number;
  serverStagedPackages: ServerStagedPackage[];
}

export interface AcceptedPackage {
  packageId: number;
  packageSignature: Uint8Array;
  uploadEndpoint: string;
}

export interface LocallyAvailablePackage {
  packageId: number;
  packageSignature: Uint8Array;
  fileSizeBytes: number;
}

function createBaseFirmwareVersion(): FirmwareVersion {
  return { version: "", githash: new Uint8Array(0) };
}

export const FirmwareVersion: MessageFns<FirmwareVersion> = {
  encode(message: FirmwareVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.githash.length !== 0) {
      writer.uint32(18).bytes(message.githash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FirmwareVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFirmwareVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.githash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FirmwareVersion {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      githash: isSet(object.githash) ? bytesFromBase64(object.githash) : new Uint8Array(0),
    };
  },

  toJSON(message: FirmwareVersion): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    if (message.githash !== undefined) {
      obj.githash = base64FromBytes(message.githash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FirmwareVersion>, I>>(base?: I): FirmwareVersion {
    return FirmwareVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FirmwareVersion>, I>>(object: I): FirmwareVersion {
    const message = createBaseFirmwareVersion();
    message.version = object.version ?? "";
    message.githash = object.githash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseServerStagedPackage(): ServerStagedPackage {
  return { downloadUrl: "", packageId: 0, packageSignature: new Uint8Array(0), serverStagedVersion: undefined };
}

export const ServerStagedPackage: MessageFns<ServerStagedPackage> = {
  encode(message: ServerStagedPackage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.downloadUrl !== "") {
      writer.uint32(10).string(message.downloadUrl);
    }
    if (message.packageId !== 0) {
      writer.uint32(16).uint64(message.packageId);
    }
    if (message.packageSignature.length !== 0) {
      writer.uint32(26).bytes(message.packageSignature);
    }
    if (message.serverStagedVersion !== undefined) {
      FirmwareVersion.encode(message.serverStagedVersion, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerStagedPackage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerStagedPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.packageId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.packageSignature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serverStagedVersion = FirmwareVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerStagedPackage {
    return {
      downloadUrl: isSet(object.downloadUrl) ? globalThis.String(object.downloadUrl) : "",
      packageId: isSet(object.packageId) ? globalThis.Number(object.packageId) : 0,
      packageSignature: isSet(object.packageSignature) ? bytesFromBase64(object.packageSignature) : new Uint8Array(0),
      serverStagedVersion: isSet(object.serverStagedVersion)
        ? FirmwareVersion.fromJSON(object.serverStagedVersion)
        : undefined,
    };
  },

  toJSON(message: ServerStagedPackage): unknown {
    const obj: any = {};
    if (message.downloadUrl !== undefined) {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.packageId !== undefined) {
      obj.packageId = Math.round(message.packageId);
    }
    if (message.packageSignature !== undefined) {
      obj.packageSignature = base64FromBytes(message.packageSignature);
    }
    if (message.serverStagedVersion !== undefined) {
      obj.serverStagedVersion = FirmwareVersion.toJSON(message.serverStagedVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerStagedPackage>, I>>(base?: I): ServerStagedPackage {
    return ServerStagedPackage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerStagedPackage>, I>>(object: I): ServerStagedPackage {
    const message = createBaseServerStagedPackage();
    message.downloadUrl = object.downloadUrl ?? "";
    message.packageId = object.packageId ?? 0;
    message.packageSignature = object.packageSignature ?? new Uint8Array(0);
    message.serverStagedVersion = (object.serverStagedVersion !== undefined && object.serverStagedVersion !== null)
      ? FirmwareVersion.fromPartial(object.serverStagedVersion)
      : undefined;
    return message;
  },
};

function createBaseSystemUpdate(): SystemUpdate {
  return {
    handshakeResult: 0,
    updateStatus: 0,
    serverStagedVersion: undefined,
    totalBytes: 0,
    bytesOffset: 0,
    estimatedBytesPerSecond: 0,
    lastHandshakeTimestamp: 0,
    lastUpdateResult: 0,
    serverStagedPackages: [],
  };
}

export const SystemUpdate: MessageFns<SystemUpdate> = {
  encode(message: SystemUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handshakeResult !== 0) {
      writer.uint32(8).int32(message.handshakeResult);
    }
    if (message.updateStatus !== 0) {
      writer.uint32(16).int32(message.updateStatus);
    }
    if (message.serverStagedVersion !== undefined) {
      FirmwareVersion.encode(message.serverStagedVersion, writer.uint32(26).fork()).join();
    }
    if (message.totalBytes !== 0) {
      writer.uint32(32).uint64(message.totalBytes);
    }
    if (message.bytesOffset !== 0) {
      writer.uint32(40).uint64(message.bytesOffset);
    }
    if (message.estimatedBytesPerSecond !== 0) {
      writer.uint32(48).uint64(message.estimatedBytesPerSecond);
    }
    if (message.lastHandshakeTimestamp !== 0) {
      writer.uint32(56).uint64(message.lastHandshakeTimestamp);
    }
    if (message.lastUpdateResult !== 0) {
      writer.uint32(64).int32(message.lastUpdateResult);
    }
    for (const v of message.serverStagedPackages) {
      ServerStagedPackage.encode(v!, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SystemUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.handshakeResult = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.updateStatus = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serverStagedVersion = FirmwareVersion.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalBytes = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.bytesOffset = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.estimatedBytesPerSecond = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.lastHandshakeTimestamp = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lastUpdateResult = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.serverStagedPackages.push(ServerStagedPackage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SystemUpdate {
    return {
      handshakeResult: isSet(object.handshakeResult) ? globalThis.Number(object.handshakeResult) : 0,
      updateStatus: isSet(object.updateStatus) ? globalThis.Number(object.updateStatus) : 0,
      serverStagedVersion: isSet(object.serverStagedVersion)
        ? FirmwareVersion.fromJSON(object.serverStagedVersion)
        : undefined,
      totalBytes: isSet(object.totalBytes) ? globalThis.Number(object.totalBytes) : 0,
      bytesOffset: isSet(object.bytesOffset) ? globalThis.Number(object.bytesOffset) : 0,
      estimatedBytesPerSecond: isSet(object.estimatedBytesPerSecond)
        ? globalThis.Number(object.estimatedBytesPerSecond)
        : 0,
      lastHandshakeTimestamp: isSet(object.lastHandshakeTimestamp)
        ? globalThis.Number(object.lastHandshakeTimestamp)
        : 0,
      lastUpdateResult: isSet(object.lastUpdateResult) ? globalThis.Number(object.lastUpdateResult) : 0,
      serverStagedPackages: globalThis.Array.isArray(object?.serverStagedPackages)
        ? object.serverStagedPackages.map((e: any) => ServerStagedPackage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SystemUpdate): unknown {
    const obj: any = {};
    if (message.handshakeResult !== undefined) {
      obj.handshakeResult = Math.round(message.handshakeResult);
    }
    if (message.updateStatus !== undefined) {
      obj.updateStatus = Math.round(message.updateStatus);
    }
    if (message.serverStagedVersion !== undefined) {
      obj.serverStagedVersion = FirmwareVersion.toJSON(message.serverStagedVersion);
    }
    if (message.totalBytes !== undefined) {
      obj.totalBytes = Math.round(message.totalBytes);
    }
    if (message.bytesOffset !== undefined) {
      obj.bytesOffset = Math.round(message.bytesOffset);
    }
    if (message.estimatedBytesPerSecond !== undefined) {
      obj.estimatedBytesPerSecond = Math.round(message.estimatedBytesPerSecond);
    }
    if (message.lastHandshakeTimestamp !== undefined) {
      obj.lastHandshakeTimestamp = Math.round(message.lastHandshakeTimestamp);
    }
    if (message.lastUpdateResult !== undefined) {
      obj.lastUpdateResult = Math.round(message.lastUpdateResult);
    }
    if (message.serverStagedPackages?.length) {
      obj.serverStagedPackages = message.serverStagedPackages.map((e) => ServerStagedPackage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SystemUpdate>, I>>(base?: I): SystemUpdate {
    return SystemUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SystemUpdate>, I>>(object: I): SystemUpdate {
    const message = createBaseSystemUpdate();
    message.handshakeResult = object.handshakeResult ?? 0;
    message.updateStatus = object.updateStatus ?? 0;
    message.serverStagedVersion = (object.serverStagedVersion !== undefined && object.serverStagedVersion !== null)
      ? FirmwareVersion.fromPartial(object.serverStagedVersion)
      : undefined;
    message.totalBytes = object.totalBytes ?? 0;
    message.bytesOffset = object.bytesOffset ?? 0;
    message.estimatedBytesPerSecond = object.estimatedBytesPerSecond ?? 0;
    message.lastHandshakeTimestamp = object.lastHandshakeTimestamp ?? 0;
    message.lastUpdateResult = object.lastUpdateResult ?? 0;
    message.serverStagedPackages = object.serverStagedPackages?.map((e) => ServerStagedPackage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAcceptedPackage(): AcceptedPackage {
  return { packageId: 0, packageSignature: new Uint8Array(0), uploadEndpoint: "" };
}

export const AcceptedPackage: MessageFns<AcceptedPackage> = {
  encode(message: AcceptedPackage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.packageId !== 0) {
      writer.uint32(8).uint64(message.packageId);
    }
    if (message.packageSignature.length !== 0) {
      writer.uint32(18).bytes(message.packageSignature);
    }
    if (message.uploadEndpoint !== "") {
      writer.uint32(26).string(message.uploadEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AcceptedPackage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptedPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.packageId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.packageSignature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadEndpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AcceptedPackage {
    return {
      packageId: isSet(object.packageId) ? globalThis.Number(object.packageId) : 0,
      packageSignature: isSet(object.packageSignature) ? bytesFromBase64(object.packageSignature) : new Uint8Array(0),
      uploadEndpoint: isSet(object.uploadEndpoint) ? globalThis.String(object.uploadEndpoint) : "",
    };
  },

  toJSON(message: AcceptedPackage): unknown {
    const obj: any = {};
    if (message.packageId !== undefined) {
      obj.packageId = Math.round(message.packageId);
    }
    if (message.packageSignature !== undefined) {
      obj.packageSignature = base64FromBytes(message.packageSignature);
    }
    if (message.uploadEndpoint !== undefined) {
      obj.uploadEndpoint = message.uploadEndpoint;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AcceptedPackage>, I>>(base?: I): AcceptedPackage {
    return AcceptedPackage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AcceptedPackage>, I>>(object: I): AcceptedPackage {
    const message = createBaseAcceptedPackage();
    message.packageId = object.packageId ?? 0;
    message.packageSignature = object.packageSignature ?? new Uint8Array(0);
    message.uploadEndpoint = object.uploadEndpoint ?? "";
    return message;
  },
};

function createBaseLocallyAvailablePackage(): LocallyAvailablePackage {
  return { packageId: 0, packageSignature: new Uint8Array(0), fileSizeBytes: 0 };
}

export const LocallyAvailablePackage: MessageFns<LocallyAvailablePackage> = {
  encode(message: LocallyAvailablePackage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.packageId !== 0) {
      writer.uint32(8).uint64(message.packageId);
    }
    if (message.packageSignature.length !== 0) {
      writer.uint32(18).bytes(message.packageSignature);
    }
    if (message.fileSizeBytes !== 0) {
      writer.uint32(24).uint64(message.fileSizeBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocallyAvailablePackage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocallyAvailablePackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.packageId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.packageSignature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fileSizeBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LocallyAvailablePackage {
    return {
      packageId: isSet(object.packageId) ? globalThis.Number(object.packageId) : 0,
      packageSignature: isSet(object.packageSignature) ? bytesFromBase64(object.packageSignature) : new Uint8Array(0),
      fileSizeBytes: isSet(object.fileSizeBytes) ? globalThis.Number(object.fileSizeBytes) : 0,
    };
  },

  toJSON(message: LocallyAvailablePackage): unknown {
    const obj: any = {};
    if (message.packageId !== undefined) {
      obj.packageId = Math.round(message.packageId);
    }
    if (message.packageSignature !== undefined) {
      obj.packageSignature = base64FromBytes(message.packageSignature);
    }
    if (message.fileSizeBytes !== undefined) {
      obj.fileSizeBytes = Math.round(message.fileSizeBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LocallyAvailablePackage>, I>>(base?: I): LocallyAvailablePackage {
    return LocallyAvailablePackage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocallyAvailablePackage>, I>>(object: I): LocallyAvailablePackage {
    const message = createBaseLocallyAvailablePackage();
    message.packageId = object.packageId ?? 0;
    message.packageSignature = object.packageSignature ?? new Uint8Array(0);
    message.fileSizeBytes = object.fileSizeBytes ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
