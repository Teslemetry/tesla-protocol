// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: transport.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AuthorizationMessages } from "./authorization_api.js";
import {
  AuthorizedClientType,
  authorizedClientTypeFromJSON,
  authorizedClientTypeToJSON,
} from "./authorization_types.js";
import { CommonMessages } from "./common_api.js";
import { TEGMessages } from "./teg_api.js";

export const protobufPackage = "tesla.proto.energy_device.v1";

/**
 * TESLEMETRY SOURCE-OF-RECORD - no upstream.
 * Provenance: Tesla Energy gateway (TEG) local API, from our own
 * observations and contributions from the community
 */

export enum DeliveryChannel {
  DELIVERY_CHANNEL_INVALID = 0,
  DELIVERY_CHANNEL_LOCAL_HTTPS = 1,
  DELIVERY_CHANNEL_HERMES_COMMAND = 2,
  DELIVERY_CHANNEL_BLE = 3,
  UNRECOGNIZED = -1,
}

export function deliveryChannelFromJSON(object: any): DeliveryChannel {
  switch (object) {
    case 0:
    case "DELIVERY_CHANNEL_INVALID":
      return DeliveryChannel.DELIVERY_CHANNEL_INVALID;
    case 1:
    case "DELIVERY_CHANNEL_LOCAL_HTTPS":
      return DeliveryChannel.DELIVERY_CHANNEL_LOCAL_HTTPS;
    case 2:
    case "DELIVERY_CHANNEL_HERMES_COMMAND":
      return DeliveryChannel.DELIVERY_CHANNEL_HERMES_COMMAND;
    case 3:
    case "DELIVERY_CHANNEL_BLE":
      return DeliveryChannel.DELIVERY_CHANNEL_BLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeliveryChannel.UNRECOGNIZED;
  }
}

export function deliveryChannelToJSON(object: DeliveryChannel): string {
  switch (object) {
    case DeliveryChannel.DELIVERY_CHANNEL_INVALID:
      return "DELIVERY_CHANNEL_INVALID";
    case DeliveryChannel.DELIVERY_CHANNEL_LOCAL_HTTPS:
      return "DELIVERY_CHANNEL_LOCAL_HTTPS";
    case DeliveryChannel.DELIVERY_CHANNEL_HERMES_COMMAND:
      return "DELIVERY_CHANNEL_HERMES_COMMAND";
    case DeliveryChannel.DELIVERY_CHANNEL_BLE:
      return "DELIVERY_CHANNEL_BLE";
    case DeliveryChannel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum TeslaService {
  TESLA_SERVICE_INVALID = 0,
  TESLA_SERVICE_COMMAND = 1,
  UNRECOGNIZED = -1,
}

export function teslaServiceFromJSON(object: any): TeslaService {
  switch (object) {
    case 0:
    case "TESLA_SERVICE_INVALID":
      return TeslaService.TESLA_SERVICE_INVALID;
    case 1:
    case "TESLA_SERVICE_COMMAND":
      return TeslaService.TESLA_SERVICE_COMMAND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TeslaService.UNRECOGNIZED;
  }
}

export function teslaServiceToJSON(object: TeslaService): string {
  switch (object) {
    case TeslaService.TESLA_SERVICE_INVALID:
      return "TESLA_SERVICE_INVALID";
    case TeslaService.TESLA_SERVICE_COMMAND:
      return "TESLA_SERVICE_COMMAND";
    case TeslaService.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum LocalParticipant {
  LOCAL_PARTICIPANT_INVALID = 0,
  LOCAL_PARTICIPANT_INSTALLER = 1,
  LOCAL_PARTICIPANT_CUSTOMER = 2,
  UNRECOGNIZED = -1,
}

export function localParticipantFromJSON(object: any): LocalParticipant {
  switch (object) {
    case 0:
    case "LOCAL_PARTICIPANT_INVALID":
      return LocalParticipant.LOCAL_PARTICIPANT_INVALID;
    case 1:
    case "LOCAL_PARTICIPANT_INSTALLER":
      return LocalParticipant.LOCAL_PARTICIPANT_INSTALLER;
    case 2:
    case "LOCAL_PARTICIPANT_CUSTOMER":
      return LocalParticipant.LOCAL_PARTICIPANT_CUSTOMER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LocalParticipant.UNRECOGNIZED;
  }
}

export function localParticipantToJSON(object: LocalParticipant): string {
  switch (object) {
    case LocalParticipant.LOCAL_PARTICIPANT_INVALID:
      return "LOCAL_PARTICIPANT_INVALID";
    case LocalParticipant.LOCAL_PARTICIPANT_INSTALLER:
      return "LOCAL_PARTICIPANT_INSTALLER";
    case LocalParticipant.LOCAL_PARTICIPANT_CUSTOMER:
      return "LOCAL_PARTICIPANT_CUSTOMER";
    case LocalParticipant.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ExternalAuthType {
  EXTERNAL_AUTH_TYPE_INVALID = 0,
  EXTERNAL_AUTH_TYPE_PRESENCE = 1,
  EXTERNAL_AUTH_TYPE_MTLS = 2,
  EXTERNAL_AUTH_TYPE_HERMES_COMMAND = 4,
  UNRECOGNIZED = -1,
}

export function externalAuthTypeFromJSON(object: any): ExternalAuthType {
  switch (object) {
    case 0:
    case "EXTERNAL_AUTH_TYPE_INVALID":
      return ExternalAuthType.EXTERNAL_AUTH_TYPE_INVALID;
    case 1:
    case "EXTERNAL_AUTH_TYPE_PRESENCE":
      return ExternalAuthType.EXTERNAL_AUTH_TYPE_PRESENCE;
    case 2:
    case "EXTERNAL_AUTH_TYPE_MTLS":
      return ExternalAuthType.EXTERNAL_AUTH_TYPE_MTLS;
    case 4:
    case "EXTERNAL_AUTH_TYPE_HERMES_COMMAND":
      return ExternalAuthType.EXTERNAL_AUTH_TYPE_HERMES_COMMAND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExternalAuthType.UNRECOGNIZED;
  }
}

export function externalAuthTypeToJSON(object: ExternalAuthType): string {
  switch (object) {
    case ExternalAuthType.EXTERNAL_AUTH_TYPE_INVALID:
      return "EXTERNAL_AUTH_TYPE_INVALID";
    case ExternalAuthType.EXTERNAL_AUTH_TYPE_PRESENCE:
      return "EXTERNAL_AUTH_TYPE_PRESENCE";
    case ExternalAuthType.EXTERNAL_AUTH_TYPE_MTLS:
      return "EXTERNAL_AUTH_TYPE_MTLS";
    case ExternalAuthType.EXTERNAL_AUTH_TYPE_HERMES_COMMAND:
      return "EXTERNAL_AUTH_TYPE_HERMES_COMMAND";
    case ExternalAuthType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ExternalAuth {
  type: ExternalAuthType;
}

export interface AuthEnvelope {
  payload: Uint8Array;
  externalAuth?: ExternalAuth | undefined;
}

export interface Participant {
  /** Device Identification Number (DIN) */
  din?: string | undefined;
  teslaService?: TeslaService | undefined;
  local?: LocalParticipant | undefined;
  authorizedClient?: AuthorizedClientType | undefined;
}

export interface RegistrationPayload {
  customerRegistrationInfo: Uint8Array;
  nonce: Uint8Array;
}

export interface MessageEnvelope {
  deliveryChannel: DeliveryChannel;
  sender: Participant | undefined;
  recipient: Participant | undefined;
  common?: CommonMessages | undefined;
  teg?:
    | TEGMessages
    | undefined;
  /**
   * WCMessages wc                       = 6;
   * NeurioMeterMessages neuriometer     = 9;
   * EnergySiteNetMessages energysitenet = 10;
   */
  authorization?: AuthorizationMessages | undefined;
}

function createBaseExternalAuth(): ExternalAuth {
  return { type: 0 };
}

export const ExternalAuth: MessageFns<ExternalAuth> = {
  encode(message: ExternalAuth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExternalAuth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExternalAuth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExternalAuth {
    return { type: isSet(object.type) ? externalAuthTypeFromJSON(object.type) : 0 };
  },

  toJSON(message: ExternalAuth): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = externalAuthTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExternalAuth>, I>>(base?: I): ExternalAuth {
    return ExternalAuth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExternalAuth>, I>>(object: I): ExternalAuth {
    const message = createBaseExternalAuth();
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseAuthEnvelope(): AuthEnvelope {
  return { payload: new Uint8Array(0), externalAuth: undefined };
}

export const AuthEnvelope: MessageFns<AuthEnvelope> = {
  encode(message: AuthEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.externalAuth !== undefined) {
      ExternalAuth.encode(message.externalAuth, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.externalAuth = ExternalAuth.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthEnvelope {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      externalAuth: isSet(object.externalAuth) ? ExternalAuth.fromJSON(object.externalAuth) : undefined,
    };
  },

  toJSON(message: AuthEnvelope): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.externalAuth !== undefined) {
      obj.externalAuth = ExternalAuth.toJSON(message.externalAuth);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthEnvelope>, I>>(base?: I): AuthEnvelope {
    return AuthEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthEnvelope>, I>>(object: I): AuthEnvelope {
    const message = createBaseAuthEnvelope();
    message.payload = object.payload ?? new Uint8Array(0);
    message.externalAuth = (object.externalAuth !== undefined && object.externalAuth !== null)
      ? ExternalAuth.fromPartial(object.externalAuth)
      : undefined;
    return message;
  },
};

function createBaseParticipant(): Participant {
  return { din: undefined, teslaService: undefined, local: undefined, authorizedClient: undefined };
}

export const Participant: MessageFns<Participant> = {
  encode(message: Participant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.din !== undefined) {
      writer.uint32(10).string(message.din);
    }
    if (message.teslaService !== undefined) {
      writer.uint32(16).int32(message.teslaService);
    }
    if (message.local !== undefined) {
      writer.uint32(24).int32(message.local);
    }
    if (message.authorizedClient !== undefined) {
      writer.uint32(32).int32(message.authorizedClient);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Participant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.din = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.teslaService = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.local = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.authorizedClient = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Participant {
    return {
      din: isSet(object.din) ? globalThis.String(object.din) : undefined,
      teslaService: isSet(object.teslaService) ? teslaServiceFromJSON(object.teslaService) : undefined,
      local: isSet(object.local) ? localParticipantFromJSON(object.local) : undefined,
      authorizedClient: isSet(object.authorizedClient)
        ? authorizedClientTypeFromJSON(object.authorizedClient)
        : undefined,
    };
  },

  toJSON(message: Participant): unknown {
    const obj: any = {};
    if (message.din !== undefined) {
      obj.din = message.din;
    }
    if (message.teslaService !== undefined) {
      obj.teslaService = teslaServiceToJSON(message.teslaService);
    }
    if (message.local !== undefined) {
      obj.local = localParticipantToJSON(message.local);
    }
    if (message.authorizedClient !== undefined) {
      obj.authorizedClient = authorizedClientTypeToJSON(message.authorizedClient);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Participant>, I>>(base?: I): Participant {
    return Participant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Participant>, I>>(object: I): Participant {
    const message = createBaseParticipant();
    message.din = object.din ?? undefined;
    message.teslaService = object.teslaService ?? undefined;
    message.local = object.local ?? undefined;
    message.authorizedClient = object.authorizedClient ?? undefined;
    return message;
  },
};

function createBaseRegistrationPayload(): RegistrationPayload {
  return { customerRegistrationInfo: new Uint8Array(0), nonce: new Uint8Array(0) };
}

export const RegistrationPayload: MessageFns<RegistrationPayload> = {
  encode(message: RegistrationPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.customerRegistrationInfo.length !== 0) {
      writer.uint32(10).bytes(message.customerRegistrationInfo);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(18).bytes(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegistrationPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegistrationPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.customerRegistrationInfo = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegistrationPayload {
    return {
      customerRegistrationInfo: isSet(object.customerRegistrationInfo)
        ? bytesFromBase64(object.customerRegistrationInfo)
        : new Uint8Array(0),
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
    };
  },

  toJSON(message: RegistrationPayload): unknown {
    const obj: any = {};
    if (message.customerRegistrationInfo !== undefined) {
      obj.customerRegistrationInfo = base64FromBytes(message.customerRegistrationInfo);
    }
    if (message.nonce !== undefined) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegistrationPayload>, I>>(base?: I): RegistrationPayload {
    return RegistrationPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegistrationPayload>, I>>(object: I): RegistrationPayload {
    const message = createBaseRegistrationPayload();
    message.customerRegistrationInfo = object.customerRegistrationInfo ?? new Uint8Array(0);
    message.nonce = object.nonce ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMessageEnvelope(): MessageEnvelope {
  return {
    deliveryChannel: 0,
    sender: undefined,
    recipient: undefined,
    common: undefined,
    teg: undefined,
    authorization: undefined,
  };
}

export const MessageEnvelope: MessageFns<MessageEnvelope> = {
  encode(message: MessageEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deliveryChannel !== 0) {
      writer.uint32(8).int32(message.deliveryChannel);
    }
    if (message.sender !== undefined) {
      Participant.encode(message.sender, writer.uint32(18).fork()).join();
    }
    if (message.recipient !== undefined) {
      Participant.encode(message.recipient, writer.uint32(26).fork()).join();
    }
    if (message.common !== undefined) {
      CommonMessages.encode(message.common, writer.uint32(34).fork()).join();
    }
    if (message.teg !== undefined) {
      TEGMessages.encode(message.teg, writer.uint32(42).fork()).join();
    }
    if (message.authorization !== undefined) {
      AuthorizationMessages.encode(message.authorization, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deliveryChannel = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sender = Participant.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recipient = Participant.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.common = CommonMessages.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.teg = TEGMessages.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.authorization = AuthorizationMessages.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageEnvelope {
    return {
      deliveryChannel: isSet(object.deliveryChannel) ? deliveryChannelFromJSON(object.deliveryChannel) : 0,
      sender: isSet(object.sender) ? Participant.fromJSON(object.sender) : undefined,
      recipient: isSet(object.recipient) ? Participant.fromJSON(object.recipient) : undefined,
      common: isSet(object.common) ? CommonMessages.fromJSON(object.common) : undefined,
      teg: isSet(object.teg) ? TEGMessages.fromJSON(object.teg) : undefined,
      authorization: isSet(object.authorization) ? AuthorizationMessages.fromJSON(object.authorization) : undefined,
    };
  },

  toJSON(message: MessageEnvelope): unknown {
    const obj: any = {};
    if (message.deliveryChannel !== undefined) {
      obj.deliveryChannel = deliveryChannelToJSON(message.deliveryChannel);
    }
    if (message.sender !== undefined) {
      obj.sender = Participant.toJSON(message.sender);
    }
    if (message.recipient !== undefined) {
      obj.recipient = Participant.toJSON(message.recipient);
    }
    if (message.common !== undefined) {
      obj.common = CommonMessages.toJSON(message.common);
    }
    if (message.teg !== undefined) {
      obj.teg = TEGMessages.toJSON(message.teg);
    }
    if (message.authorization !== undefined) {
      obj.authorization = AuthorizationMessages.toJSON(message.authorization);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageEnvelope>, I>>(base?: I): MessageEnvelope {
    return MessageEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageEnvelope>, I>>(object: I): MessageEnvelope {
    const message = createBaseMessageEnvelope();
    message.deliveryChannel = object.deliveryChannel ?? 0;
    message.sender = (object.sender !== undefined && object.sender !== null)
      ? Participant.fromPartial(object.sender)
      : undefined;
    message.recipient = (object.recipient !== undefined && object.recipient !== null)
      ? Participant.fromPartial(object.recipient)
      : undefined;
    message.common = (object.common !== undefined && object.common !== null)
      ? CommonMessages.fromPartial(object.common)
      : undefined;
    message.teg = (object.teg !== undefined && object.teg !== null) ? TEGMessages.fromPartial(object.teg) : undefined;
    message.authorization = (object.authorization !== undefined && object.authorization !== null)
      ? AuthorizationMessages.fromPartial(object.authorization)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
