// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: graphql_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { StringValue } from "./google/protobuf/wrappers.js";

export const protobufPackage = "tesla.proto.energy_device.v1";

/**
 * TESLEMETRY SOURCE-OF-RECORD - no upstream.
 * Provenance: Tesla Energy gateway (TEG) local API, from our own
 * observations and contributions from the community
 */

export enum GraphQLQueryFormat {
  GRAPH_QL_QUERY_FORMAT_INVALID = 0,
  GRAPH_QL_QUERY_FORMAT_RAW = 1,
  GRAPH_QL_QUERY_FORMAT_SIGNED_SHA256_ECDSA_ASN1 = 2,
  UNRECOGNIZED = -1,
}

export function graphQLQueryFormatFromJSON(object: any): GraphQLQueryFormat {
  switch (object) {
    case 0:
    case "GRAPH_QL_QUERY_FORMAT_INVALID":
      return GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_INVALID;
    case 1:
    case "GRAPH_QL_QUERY_FORMAT_RAW":
      return GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_RAW;
    case 2:
    case "GRAPH_QL_QUERY_FORMAT_SIGNED_SHA256_ECDSA_ASN1":
      return GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_SIGNED_SHA256_ECDSA_ASN1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GraphQLQueryFormat.UNRECOGNIZED;
  }
}

export function graphQLQueryFormatToJSON(object: GraphQLQueryFormat): string {
  switch (object) {
    case GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_INVALID:
      return "GRAPH_QL_QUERY_FORMAT_INVALID";
    case GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_RAW:
      return "GRAPH_QL_QUERY_FORMAT_RAW";
    case GraphQLQueryFormat.GRAPH_QL_QUERY_FORMAT_SIGNED_SHA256_ECDSA_ASN1:
      return "GRAPH_QL_QUERY_FORMAT_SIGNED_SHA256_ECDSA_ASN1";
    case GraphQLQueryFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum GraphQLStatus {
  GRAPH_QL_STATUS_INVALID = 0,
  GRAPH_QL_STATUS_SUCCESS = 1,
  GRAPH_QL_STATUS_ERROR_PARTIAL = 2,
  GRAPH_QL_STATUS_ERROR_FULL = 3,
  UNRECOGNIZED = -1,
}

export function graphQLStatusFromJSON(object: any): GraphQLStatus {
  switch (object) {
    case 0:
    case "GRAPH_QL_STATUS_INVALID":
      return GraphQLStatus.GRAPH_QL_STATUS_INVALID;
    case 1:
    case "GRAPH_QL_STATUS_SUCCESS":
      return GraphQLStatus.GRAPH_QL_STATUS_SUCCESS;
    case 2:
    case "GRAPH_QL_STATUS_ERROR_PARTIAL":
      return GraphQLStatus.GRAPH_QL_STATUS_ERROR_PARTIAL;
    case 3:
    case "GRAPH_QL_STATUS_ERROR_FULL":
      return GraphQLStatus.GRAPH_QL_STATUS_ERROR_FULL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GraphQLStatus.UNRECOGNIZED;
  }
}

export function graphQLStatusToJSON(object: GraphQLStatus): string {
  switch (object) {
    case GraphQLStatus.GRAPH_QL_STATUS_INVALID:
      return "GRAPH_QL_STATUS_INVALID";
    case GraphQLStatus.GRAPH_QL_STATUS_SUCCESS:
      return "GRAPH_QL_STATUS_SUCCESS";
    case GraphQLStatus.GRAPH_QL_STATUS_ERROR_PARTIAL:
      return "GRAPH_QL_STATUS_ERROR_PARTIAL";
    case GraphQLStatus.GRAPH_QL_STATUS_ERROR_FULL:
      return "GRAPH_QL_STATUS_ERROR_FULL";
    case GraphQLStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum GraphQLErrorCode {
  GRAPH_QL_ERROR_CODE_INVALID = 0,
  GRAPH_QL_ERROR_CODE_FIELD_NOT_FOUND = 1,
  GRAPH_QL_ERROR_CODE_UNAUTHORIZED = 2,
  GRAPH_QL_ERROR_CODE_RESOLVER_PROCESS_UNRESPONSIVE = 3,
  GRAPH_QL_ERROR_CODE_RESPONSE_TOO_BIG = 4,
  UNRECOGNIZED = -1,
}

export function graphQLErrorCodeFromJSON(object: any): GraphQLErrorCode {
  switch (object) {
    case 0:
    case "GRAPH_QL_ERROR_CODE_INVALID":
      return GraphQLErrorCode.GRAPH_QL_ERROR_CODE_INVALID;
    case 1:
    case "GRAPH_QL_ERROR_CODE_FIELD_NOT_FOUND":
      return GraphQLErrorCode.GRAPH_QL_ERROR_CODE_FIELD_NOT_FOUND;
    case 2:
    case "GRAPH_QL_ERROR_CODE_UNAUTHORIZED":
      return GraphQLErrorCode.GRAPH_QL_ERROR_CODE_UNAUTHORIZED;
    case 3:
    case "GRAPH_QL_ERROR_CODE_RESOLVER_PROCESS_UNRESPONSIVE":
      return GraphQLErrorCode.GRAPH_QL_ERROR_CODE_RESOLVER_PROCESS_UNRESPONSIVE;
    case 4:
    case "GRAPH_QL_ERROR_CODE_RESPONSE_TOO_BIG":
      return GraphQLErrorCode.GRAPH_QL_ERROR_CODE_RESPONSE_TOO_BIG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GraphQLErrorCode.UNRECOGNIZED;
  }
}

export function graphQLErrorCodeToJSON(object: GraphQLErrorCode): string {
  switch (object) {
    case GraphQLErrorCode.GRAPH_QL_ERROR_CODE_INVALID:
      return "GRAPH_QL_ERROR_CODE_INVALID";
    case GraphQLErrorCode.GRAPH_QL_ERROR_CODE_FIELD_NOT_FOUND:
      return "GRAPH_QL_ERROR_CODE_FIELD_NOT_FOUND";
    case GraphQLErrorCode.GRAPH_QL_ERROR_CODE_UNAUTHORIZED:
      return "GRAPH_QL_ERROR_CODE_UNAUTHORIZED";
    case GraphQLErrorCode.GRAPH_QL_ERROR_CODE_RESOLVER_PROCESS_UNRESPONSIVE:
      return "GRAPH_QL_ERROR_CODE_RESOLVER_PROCESS_UNRESPONSIVE";
    case GraphQLErrorCode.GRAPH_QL_ERROR_CODE_RESPONSE_TOO_BIG:
      return "GRAPH_QL_ERROR_CODE_RESPONSE_TOO_BIG";
    case GraphQLErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SignedGraphQLQuery {
  version: number;
  query: Uint8Array;
}

export interface GraphQLError {
  path: string[];
  code: GraphQLErrorCode;
  message: string;
}

export interface GraphQLAPIQueryRequest {
  format: GraphQLQueryFormat;
  query: Uint8Array;
  signature: Uint8Array;
  variablesJson: string | undefined;
}

export interface GraphQLAPIQueryResponse {
  status: GraphQLStatus;
  data: string;
  errors: GraphQLError[];
}

export interface GraphQLMessages {
  queryRequest?: GraphQLAPIQueryRequest | undefined;
  queryResponse?: GraphQLAPIQueryResponse | undefined;
}

function createBaseSignedGraphQLQuery(): SignedGraphQLQuery {
  return { version: 0, query: new Uint8Array(0) };
}

export const SignedGraphQLQuery: MessageFns<SignedGraphQLQuery> = {
  encode(message: SignedGraphQLQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.query.length !== 0) {
      writer.uint32(18).bytes(message.query);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignedGraphQLQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignedGraphQLQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.query = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignedGraphQLQuery {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      query: isSet(object.query) ? bytesFromBase64(object.query) : new Uint8Array(0),
    };
  },

  toJSON(message: SignedGraphQLQuery): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = Math.round(message.version);
    }
    if (message.query !== undefined) {
      obj.query = base64FromBytes(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignedGraphQLQuery>, I>>(base?: I): SignedGraphQLQuery {
    return SignedGraphQLQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignedGraphQLQuery>, I>>(object: I): SignedGraphQLQuery {
    const message = createBaseSignedGraphQLQuery();
    message.version = object.version ?? 0;
    message.query = object.query ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGraphQLError(): GraphQLError {
  return { path: [], code: 0, message: "" };
}

export const GraphQLError: MessageFns<GraphQLError> = {
  encode(message: GraphQLError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.path) {
      writer.uint32(10).string(v!);
    }
    if (message.code !== 0) {
      writer.uint32(16).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphQLError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphQLError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphQLError {
    return {
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [],
      code: isSet(object.code) ? graphQLErrorCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: GraphQLError): unknown {
    const obj: any = {};
    if (message.path?.length) {
      obj.path = message.path;
    }
    if (message.code !== undefined) {
      obj.code = graphQLErrorCodeToJSON(message.code);
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphQLError>, I>>(base?: I): GraphQLError {
    return GraphQLError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphQLError>, I>>(object: I): GraphQLError {
    const message = createBaseGraphQLError();
    message.path = object.path?.map((e) => e) || [];
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseGraphQLAPIQueryRequest(): GraphQLAPIQueryRequest {
  return { format: 0, query: new Uint8Array(0), signature: new Uint8Array(0), variablesJson: undefined };
}

export const GraphQLAPIQueryRequest: MessageFns<GraphQLAPIQueryRequest> = {
  encode(message: GraphQLAPIQueryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.format !== 0) {
      writer.uint32(8).int32(message.format);
    }
    if (message.query.length !== 0) {
      writer.uint32(18).bytes(message.query);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    if (message.variablesJson !== undefined) {
      StringValue.encode({ value: message.variablesJson! }, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphQLAPIQueryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphQLAPIQueryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.query = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.variablesJson = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphQLAPIQueryRequest {
    return {
      format: isSet(object.format) ? graphQLQueryFormatFromJSON(object.format) : 0,
      query: isSet(object.query) ? bytesFromBase64(object.query) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      variablesJson: isSet(object.variablesJson) ? String(object.variablesJson) : undefined,
    };
  },

  toJSON(message: GraphQLAPIQueryRequest): unknown {
    const obj: any = {};
    if (message.format !== undefined) {
      obj.format = graphQLQueryFormatToJSON(message.format);
    }
    if (message.query !== undefined) {
      obj.query = base64FromBytes(message.query);
    }
    if (message.signature !== undefined) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.variablesJson !== undefined) {
      obj.variablesJson = message.variablesJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphQLAPIQueryRequest>, I>>(base?: I): GraphQLAPIQueryRequest {
    return GraphQLAPIQueryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphQLAPIQueryRequest>, I>>(object: I): GraphQLAPIQueryRequest {
    const message = createBaseGraphQLAPIQueryRequest();
    message.format = object.format ?? 0;
    message.query = object.query ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.variablesJson = object.variablesJson ?? undefined;
    return message;
  },
};

function createBaseGraphQLAPIQueryResponse(): GraphQLAPIQueryResponse {
  return { status: 0, data: "", errors: [] };
}

export const GraphQLAPIQueryResponse: MessageFns<GraphQLAPIQueryResponse> = {
  encode(message: GraphQLAPIQueryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.data !== "") {
      writer.uint32(18).string(message.data);
    }
    for (const v of message.errors) {
      GraphQLError.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphQLAPIQueryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphQLAPIQueryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(GraphQLError.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphQLAPIQueryResponse {
    return {
      status: isSet(object.status) ? graphQLStatusFromJSON(object.status) : 0,
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => GraphQLError.fromJSON(e)) : [],
    };
  },

  toJSON(message: GraphQLAPIQueryResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = graphQLStatusToJSON(message.status);
    }
    if (message.data !== undefined) {
      obj.data = message.data;
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => GraphQLError.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphQLAPIQueryResponse>, I>>(base?: I): GraphQLAPIQueryResponse {
    return GraphQLAPIQueryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphQLAPIQueryResponse>, I>>(object: I): GraphQLAPIQueryResponse {
    const message = createBaseGraphQLAPIQueryResponse();
    message.status = object.status ?? 0;
    message.data = object.data ?? "";
    message.errors = object.errors?.map((e) => GraphQLError.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGraphQLMessages(): GraphQLMessages {
  return { queryRequest: undefined, queryResponse: undefined };
}

export const GraphQLMessages: MessageFns<GraphQLMessages> = {
  encode(message: GraphQLMessages, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queryRequest !== undefined) {
      GraphQLAPIQueryRequest.encode(message.queryRequest, writer.uint32(10).fork()).join();
    }
    if (message.queryResponse !== undefined) {
      GraphQLAPIQueryResponse.encode(message.queryResponse, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphQLMessages {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphQLMessages();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.queryRequest = GraphQLAPIQueryRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.queryResponse = GraphQLAPIQueryResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphQLMessages {
    return {
      queryRequest: isSet(object.queryRequest) ? GraphQLAPIQueryRequest.fromJSON(object.queryRequest) : undefined,
      queryResponse: isSet(object.queryResponse) ? GraphQLAPIQueryResponse.fromJSON(object.queryResponse) : undefined,
    };
  },

  toJSON(message: GraphQLMessages): unknown {
    const obj: any = {};
    if (message.queryRequest !== undefined) {
      obj.queryRequest = GraphQLAPIQueryRequest.toJSON(message.queryRequest);
    }
    if (message.queryResponse !== undefined) {
      obj.queryResponse = GraphQLAPIQueryResponse.toJSON(message.queryResponse);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphQLMessages>, I>>(base?: I): GraphQLMessages {
    return GraphQLMessages.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphQLMessages>, I>>(object: I): GraphQLMessages {
    const message = createBaseGraphQLMessages();
    message.queryRequest = (object.queryRequest !== undefined && object.queryRequest !== null)
      ? GraphQLAPIQueryRequest.fromPartial(object.queryRequest)
      : undefined;
    message.queryResponse = (object.queryResponse !== undefined && object.queryResponse !== null)
      ? GraphQLAPIQueryResponse.fromPartial(object.queryResponse)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
