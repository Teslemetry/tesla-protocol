# Pinned codegen toolchain. The bundled protoc in grpcio-tools generates the
# committed *_pb2.py output; changing these pins changes generated code, so CI
# regenerates with exactly these versions and fails on any diff.
grpcio-tools==1.76.0
click==8.4.2
build==1.3.0
twine==6.2.0
# protoletariat is installed separately with --no-deps (see generate.sh): it
# declares protobuf<6 but only parses descriptor sets, which works on 6.x.
